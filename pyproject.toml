[project]
name = "recording-api"
version = "0.1.0"
description = "HTTP API for controlling audio recordings via ffmpeg, with Bitfocus Companion integration"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.115",
    "pydantic>=2.0",
    "requests>=2.32",
    "pyyaml>=6.0",
]
